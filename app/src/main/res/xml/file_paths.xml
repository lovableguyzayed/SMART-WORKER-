<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="payslips" path="payslips/" />
    <cache-path name="exports" path="exports/" />
    <!-- OTA: downloaded APKs live in app-specific external storage (no runtime
         storage permission needed). Shared with the installer via FileProvider. -->
    <external-files-path name="apk" path="apk/" />
</paths>
